{% extends 'base.html.twig' %}

{% block title %}New Participant{% endblock %}

{% block body %}
    <h1 style="color: #0c5460">Create new Participant</h1>

    {{ include('participant/_form.html.twig') }}

    <a href="{{ path('participant_index') }}" style="color: black">back to list</a>
{% endblock %}
